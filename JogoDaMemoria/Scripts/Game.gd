extends Control

@onready var grid: GridContainer = get_node("MarginContainer/Grid")

func _ready() -> void:
	# var v = CardEnums.Value.Jack
	# var s = CardEnums.Suit.Clubs
	# var c = Card.new(v, s)
	
	# #TODO apgar DEBUG
	# #c.position = Vector2(500, 400)
	# grid.add_child(c)
	pass
